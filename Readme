- cd frontend
- npm install
- cd ..
- cd backend
- source venv/bin/activate
- cd ..
### Run this command in object-detection directory (will take a few minutes to complete)
- docker-compose up --build